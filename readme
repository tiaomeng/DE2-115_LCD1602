基于DE2开发板的LCD显示实验
实现功能如下：
1.开机显示默认文字：开机后，LCD默认第一行显示"HELLO"，第二行显示"WORLD"。
2.按键检测功能：按下KEY1，屏幕上显示“state:/KEY1 push”; 按下KEY2，屏幕上显示“state:/KEY2 push”; 按下KEY3，屏幕上显示“state:/KEY3 push”。
3.开关状态显示：打开开关SW0，屏幕上显示SW17~SW3的开闭状态，开关拨上打开为1，开关拨下关闭为0，每约1秒刷新一次。
4.光标移动模式：打开开关SW1，屏幕上初始显示“1000000000000000”，“1”下方有一光标“^”。按下KEY2，“1”右移，且不会超过第16位；按下KEY3，“1”左移，且不会超过第1位；光标始终跟随1移动。
